from django.urls import path

# Group, expense, balance, and import routes get added here as we build
# each piece. Kept empty (not omitted) so config/urls.py's include() doesn't
# break while we're mid-build.
urlpatterns = []
